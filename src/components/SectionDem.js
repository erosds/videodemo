import React from 'react';

const SectionDem = ({ section, isActive, totalSections }) => {
  return (
    <section className="w-screen h-full flex flex-col items-center justify-center snap-center px-8 relative">
      {/* Il contenuto viene ora gestito da InteractiveContent in App.js */}
    </section>
  );
};

export default SectionDem;